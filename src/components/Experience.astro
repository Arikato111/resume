---
import type { Lang } from "../types/lang";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;

const msg = {
  exp: {
    en: "Experience",
    th: "ประสบการณ์ทำงาน",
  },
  noexp: {
    en: "No Experiences",
    th: "ไม่มีประสบการณ์",
  },
};
---

<!-- Section reserved for work experience -->

